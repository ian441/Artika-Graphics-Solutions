{
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.0",
    "@types/jest": "^30.0.0",
    "jest": "^30.2.0",
    "ts-jest": "^29.4.5",
    "typescript": "^5.9.3"
  },
  "scripts": {
    "test": "npm run test --prefix backend && npm run test --prefix client",
    "test:watch": "jest --watch",
    "test:single": "jest --runInBand"
  }
}
